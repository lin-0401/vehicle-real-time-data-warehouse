# 智能网联车辆实时指标计算与数仓建设项目
# 依赖清单

# 数据处理
pandas>=1.5.0
numpy>=1.21.0

# 实时流计算 (可选，有则用PyFlink，无则降级)
# 注意：PyPI包名是 apache-flink，不是 pyflink 或 flink
# 如果安装失败，代码会自动降级为Pandas模拟
apache-flink>=1.16.0
# kafka-python>=2.0.2  # 如需真实Kafka，取消注释

# 离线数仓 (可选，有则用PySpark，无则降级为Pandas)
pyspark>=3.3.0

# 可视化
streamlit>=1.25.0
plotly>=5.15.0

# 网络请求 (爬虫)
requests>=2.28.0

# 其他工具
openpyxl>=3.0.10  # Excel支持
xlrd>=2.0.1      # Excel读取
